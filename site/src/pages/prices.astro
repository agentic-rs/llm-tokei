---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "LLM price history | llm-tokei";
const pageDescription =
  "Explore provider-route token prices recorded in the models.dev Git history.";
const dataBaseUrl = `${import.meta.env.BASE_URL}price-history/`;
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <div class="container prices-page" data-price-app data-data-base-url={dataBaseUrl}>
    <header class="prices-hero">
      <div>
        <span class="eyebrow">Price history</span>
        <h1>What did a token cost?</h1>
      </div>
      <div class="prices-hero__copy">
        <p>
          Compare the provider-route prices recorded in the
          <a href="https://github.com/anomalyco/models.dev">models.dev</a>
          Git history. Values are USD per million tokens.
        </p>
        <p class="prices-hero__note">
          Dates show when models.dev recorded a change, which may differ from a
          provider’s actual effective date.
        </p>
      </div>
    </header>

    <section class="explorer panel" aria-labelledby="explorer-title">
      <div class="explorer__header">
        <div>
          <span class="eyebrow">Explorer</span>
          <h2 id="explorer-title">Provider-route pricing</h2>
        </div>
        <p class="status" data-status aria-live="polite">Loading price history…</p>
      </div>

      <div class="controls" data-controls hidden>
        <label>
          <span>Provider</span>
          <select data-provider aria-label="Provider"></select>
        </label>
        <fieldset data-fields>
          <legend>Price dimensions</legend>
        </fieldset>
      </div>

      <section
        class="model-legend"
        data-model-legend
        aria-labelledby="model-legend-title"
        hidden
      >
        <div class="model-legend__header">
          <div>
            <span class="eyebrow">Model legend</span>
            <h3 id="model-legend-title">Click models to show or hide lines</h3>
            <p data-model-summary></p>
          </div>
          <div class="model-legend__actions">
            <label>
              <span>Filter models</span>
              <input
                data-model-filter
                type="search"
                autocomplete="off"
                placeholder="Filter model names"
              />
            </label>
            <button
              class="model-legend__filter"
              data-eventful-models
              type="button"
              aria-pressed="false"
              aria-label="Show only models with more than one history event"
            >
              Events &gt; 1
            </button>
            <button
              data-recent-models
              type="button"
              aria-label="Plot the five newest models in the current filter"
            >
              Show recent 5
            </button>
          </div>
        </div>
        <div
          class="model-options"
          data-model-options
          role="group"
          aria-label="Models to plot"
        ></div>
      </section>

      <div class="chart-shell" data-chart-shell hidden>
        <div class="chart-heading">
          <div>
            <p class="chart-route mono" data-route></p>
            <p class="chart-range" data-range></p>
          </div>
        </div>
        <div class="metric-charts" data-charts></div>
        <div class="chart-footer">
          <p data-summary></p>
          <p>
            Filled dots start a price, rings mark changes, and × marks removal.
            Panels share time, use independent scales, and show missing values
            as gaps.
          </p>
        </div>
      </div>

      <div class="empty-state" data-empty hidden>
        <strong data-empty-title>Price history is not available in this build.</strong>
        <span data-empty-detail>
          The scheduled Pages build generates it without committing the CSV to Git.
        </span>
      </div>
    </section>

    <section class="provenance" aria-labelledby="provenance-title">
      <div>
        <span class="eyebrow">Provenance</span>
        <h2 id="provenance-title">Traceable to source.</h2>
      </div>
      <dl class="provenance__facts panel">
        <div>
          <dt>Source commit</dt>
          <dd><a data-source-commit href="https://github.com/anomalyco/models.dev">—</a></dd>
        </div>
        <div>
          <dt>History events</dt>
          <dd data-event-count>—</dd>
        </div>
        <div>
          <dt>Provider routes</dt>
          <dd data-route-count>—</dd>
        </div>
        <div>
          <dt>Download size</dt>
          <dd data-download-size>—</dd>
        </div>
      </dl>
    </section>
  </div>

  <style>
    .prices-page {
      display: grid;
      gap: clamp(3rem, 7vw, 5rem);
    }

    .prices-hero {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(22rem, 0.72fr);
      gap: clamp(2rem, 6vw, 5rem);
      align-items: end;
      padding-top: clamp(0.5rem, 3vw, 2rem);
    }

    .prices-hero > div:first-child {
      display: grid;
      gap: 1.2rem;
    }

    .prices-hero h1 {
      margin: 0;
      max-width: 10ch;
      font-size: clamp(3.2rem, 9vw, 7rem);
      line-height: 0.9;
      letter-spacing: -0.05em;
    }

    .prices-hero__copy {
      display: grid;
      gap: 1rem;
      padding-bottom: 0.4rem;
    }

    .prices-hero__copy p {
      margin: 0;
      color: var(--ink-soft);
      font-size: 1.05rem;
    }

    .prices-hero__copy a {
      color: var(--green-deep);
      text-decoration: underline;
      text-underline-offset: 0.18em;
    }

    .prices-hero__note {
      padding-left: 1rem;
      border-left: 3px solid var(--amber);
      font-size: 0.92rem !important;
    }

    .explorer {
      min-width: 0;
      overflow: hidden;
      background: rgba(255, 250, 240, 0.94);
      box-shadow: 10px 10px 0 var(--paper-deep);
    }

    .explorer__header {
      display: flex;
      justify-content: space-between;
      gap: 2rem;
      align-items: end;
      padding: clamp(1rem, 3vw, 1.5rem);
      border-bottom: 1px solid var(--line);
    }

    .explorer__header > div {
      display: grid;
      gap: 0.55rem;
    }

    .explorer h2,
    .provenance h2 {
      margin: 0;
      font-size: clamp(1.8rem, 4vw, 3.5rem);
      line-height: 1;
    }

    .status {
      margin: 0;
      color: var(--ink-muted);
      font-family: var(--font-mono);
      font-size: 0.78rem;
      text-align: right;
    }

    .controls {
      display: grid;
      grid-template-columns: minmax(10rem, 0.5fr) minmax(28rem, 2fr);
      gap: 1rem;
      padding: 1rem clamp(1rem, 3vw, 1.5rem);
      border-bottom: 1px solid var(--line);
      background: var(--panel-cool);
    }

    .controls label,
    .controls fieldset {
      display: grid;
      min-width: 0;
      gap: 0.4rem;
      margin: 0;
      padding: 0;
      border: 0;
    }

    .controls label > span:first-child,
    .controls legend {
      color: var(--ink-muted);
      font-family: var(--font-mono);
      font-size: 0.72rem;
      font-weight: 800;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .controls select,
    .controls input {
      width: 100%;
      min-width: 0;
      height: 2.75rem;
      padding: 0 0.75rem;
      border: 1px solid var(--line-strong);
      border-radius: 6px;
      color: var(--ink);
      background: var(--panel);
      font: 0.9rem var(--font-mono);
    }

    .controls fieldset {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      align-content: end;
      gap: 0.35rem 0.75rem;
    }

    .controls legend {
      grid-column: 1 / -1;
      margin-bottom: 0.4rem;
    }

    :global(.field-toggle) {
      display: flex !important;
      grid-template-columns: none !important;
      align-items: center;
      gap: 0.4rem !important;
      color: var(--ink-soft);
      font-size: 0.8rem;
      white-space: nowrap;
    }

    :global(.field-toggle input) {
      width: 0.95rem;
      height: 0.95rem;
      margin: 0;
      accent-color: var(--green);
    }

    :global(.field-toggle[data-unavailable="true"]) {
      opacity: 0.42;
    }

    .model-legend {
      padding: 1rem clamp(1rem, 3vw, 1.5rem);
      border-bottom: 1px solid var(--line);
      background: rgba(244, 239, 227, 0.72);
    }

    .model-legend__header {
      display: flex;
      align-items: end;
      justify-content: space-between;
      gap: 1.5rem;
      margin-bottom: 0.85rem;
    }

    .model-legend__header > div:first-child {
      display: grid;
      gap: 0.25rem;
    }

    .model-legend h3,
    .model-legend p {
      margin: 0;
    }

    .model-legend h3 {
      font-size: 1rem;
    }

    .model-legend p {
      color: var(--ink-muted);
      font: 0.72rem var(--font-mono);
    }

    .model-legend__actions {
      display: flex;
      align-items: end;
      gap: 0.5rem;
    }

    .model-legend__actions label {
      display: grid;
      gap: 0.25rem;
    }

    .model-legend__actions label span {
      color: var(--ink-muted);
      font: 800 0.65rem var(--font-mono);
      letter-spacing: 0.05em;
      text-transform: uppercase;
    }

    .model-legend__actions input,
    .model-legend__actions button {
      height: 2.25rem;
      border: 1px solid var(--line-strong);
      border-radius: 5px;
      font: 0.75rem var(--font-mono);
    }

    .model-legend__actions input {
      width: min(15rem, 35vw);
      padding: 0 0.65rem;
      color: var(--ink);
      background: var(--panel);
    }

    .model-legend__actions button {
      padding: 0 0.75rem;
      color: var(--panel);
      background: var(--ink);
      font-weight: 800;
      cursor: pointer;
    }

    .model-legend__actions button:hover {
      background: var(--green-deep);
    }

    .model-legend__actions button:disabled {
      opacity: 0.45;
      cursor: not-allowed;
    }

    .model-legend__actions .model-legend__filter {
      color: var(--ink);
      background: var(--panel);
    }

    .model-legend__actions .model-legend__filter:hover,
    .model-legend__actions .model-legend__filter[aria-pressed="true"] {
      border-color: var(--green-deep);
      color: var(--panel);
      background: var(--green-deep);
    }

    .model-options {
      display: flex;
      max-height: 12rem;
      flex-wrap: wrap;
      gap: 0.4rem;
      overflow: auto;
      scrollbar-gutter: stable;
    }

    :global(.model-option) {
      display: inline-flex;
      min-width: 0;
      max-width: min(100%, 23rem);
      align-items: center;
      gap: 0.4rem;
      padding: 0.36rem 0.55rem;
      border: 1px solid var(--line);
      border-radius: 5px;
      color: var(--ink-muted);
      background: rgba(255, 250, 240, 0.55);
      font: 0.72rem var(--font-mono);
      cursor: pointer;
    }

    :global(.model-option:hover) {
      border-color: var(--model-color);
      color: var(--ink);
    }

    :global(.model-option[aria-pressed="true"]) {
      border-color: var(--model-color);
      color: var(--ink);
      background: var(--panel);
      box-shadow: inset 3px 0 0 var(--model-color);
      font-weight: 800;
    }

    :global(.model-option:focus-visible) {
      outline: 3px solid var(--amber);
      outline-offset: 1px;
    }

    :global(.model-option__swatch) {
      width: 0.72rem;
      height: 0.72rem;
      flex: 0 0 auto;
      border: 2px solid var(--model-color);
      border-radius: 50%;
      background: transparent;
    }

    :global(.model-option[aria-pressed="true"] .model-option__swatch) {
      background: var(--model-color);
    }

    :global(.model-option__name) {
      min-width: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    :global(.model-option__state) {
      flex: 0 0 auto;
      color: var(--ink-muted);
      font-size: 0.62rem;
    }

    :global(.model-option__count) {
      display: inline-grid;
      width: 1.55rem;
      height: 1.55rem;
      flex: 0 0 auto;
      place-items: center;
      border: 1px solid var(--line);
      border-radius: 50%;
      color: var(--ink-soft);
      background: var(--paper-deep);
      font-size: 0.62rem;
      font-variant-numeric: tabular-nums;
    }

    :global(.model-options__empty) {
      color: var(--ink-muted);
      font: 0.75rem var(--font-mono);
    }

    .chart-shell {
      padding: clamp(1rem, 3vw, 1.5rem);
    }

    .chart-heading,
    .chart-footer {
      display: flex;
      justify-content: space-between;
      gap: 1.5rem;
    }

    .chart-heading {
      min-height: 4.5rem;
      align-items: start;
    }

    .chart-route {
      margin: 0;
      font-weight: 800;
      overflow-wrap: anywhere;
    }

    .chart-range {
      margin: 0.2rem 0 0;
      color: var(--ink-muted);
      font-size: 0.85rem;
    }

    .metric-charts {
      display: grid;
      gap: 1rem;
    }

    :global(.metric-chart) {
      min-width: 0;
      overflow: hidden;
      border: 1px solid var(--line);
      border-radius: 7px;
      background: rgba(255, 255, 255, 0.3);
    }

    :global(.metric-chart__heading) {
      display: flex;
      min-height: 3.4rem;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 0.7rem 0.9rem;
      border-bottom: 1px solid var(--line);
      background: var(--panel-cool);
    }

    :global(.metric-chart__heading > div) {
      display: flex;
      flex: 0 0 auto;
      align-items: baseline;
      gap: 0.55rem;
    }

    :global(.metric-chart__heading h3) {
      margin: 0;
      font-size: 1rem;
    }

    :global(.metric-chart__heading span),
    :global(.metric-chart__detail) {
      color: var(--ink-muted);
      font: 0.7rem/1.45 var(--font-mono);
    }

    :global(.metric-chart__detail) {
      margin: 0;
      text-align: right;
    }

    :global(.metric-chart__detail a) {
      color: var(--blue);
      font-weight: 800;
    }

    :global(.metric-chart__plot) {
      min-width: 0;
      overflow: hidden;
    }

    :global(.metric-chart__plot .uplot) {
      font-family: var(--font-mono);
    }

    :global(.metric-chart__plot .u-cursor-x) {
      border-right: 1px dashed var(--ink-muted);
    }

    :global(.change-marker) {
      position: absolute;
      z-index: 4;
      width: 0.68rem;
      height: 0.68rem;
      padding: 0;
      border: 2px solid var(--marker-color);
      border-radius: 50%;
      color: var(--marker-color);
      background: var(--panel);
      box-shadow: 0 0 0 2px rgba(255, 250, 240, 0.75);
      transform: translate(-50%, -50%);
      cursor: help;
    }

    :global(.change-marker--start) {
      background: var(--marker-color);
    }

    :global(.change-marker--remove) {
      display: grid;
      width: 0.9rem;
      height: 0.9rem;
      border: 0;
      border-radius: 0;
      place-items: center;
      background: var(--panel);
      box-shadow: none;
      font: 900 1rem/0.9 var(--font-sans);
    }

    :global(.change-marker:focus-visible) {
      outline: 3px solid var(--amber);
      outline-offset: 2px;
    }

    :global(.chart-empty) {
      display: grid;
      min-height: 16rem;
      margin: 0;
      place-items: center;
      color: var(--ink-muted);
      font: 0.85rem var(--font-mono);
    }

    .chart-footer {
      padding-top: 0.85rem;
      border-top: 1px solid var(--line);
      color: var(--ink-muted);
      font-size: 0.78rem;
    }

    .chart-footer p {
      margin: 0;
    }

    .empty-state {
      display: grid;
      gap: 0.4rem;
      padding: clamp(2rem, 7vw, 5rem) 1.5rem;
      text-align: center;
    }

    .empty-state span {
      color: var(--ink-muted);
    }

    .provenance {
      display: grid;
      grid-template-columns: minmax(0, 0.75fr) minmax(0, 1.25fr);
      gap: 2rem;
      align-items: start;
    }

    .provenance > div {
      display: grid;
      gap: 0.8rem;
    }

    .provenance__facts {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      margin: 0;
      overflow: hidden;
    }

    .provenance__facts div {
      display: grid;
      gap: 0.35rem;
      padding: 1rem;
      border-top: 1px solid var(--line);
      border-left: 1px solid var(--line);
    }

    .provenance__facts div:nth-child(-n + 2) {
      border-top: 0;
    }

    .provenance__facts div:nth-child(odd) {
      border-left: 0;
    }

    .provenance__facts dt {
      color: var(--ink-muted);
      font: 0.72rem var(--font-mono);
      text-transform: uppercase;
    }

    .provenance__facts dd {
      min-width: 0;
      margin: 0;
      font-weight: 800;
      overflow-wrap: anywhere;
    }

    .provenance__facts a {
      color: var(--blue);
    }

    @media (max-width: 900px) {
      .prices-hero,
      .provenance {
        grid-template-columns: 1fr;
      }

      .prices-hero__copy {
        max-width: 45rem;
      }

      .controls {
        grid-template-columns: 1fr 1.5fr;
      }
    }

    @media (max-width: 620px) {
      .explorer {
        box-shadow: 5px 5px 0 var(--paper-deep);
      }

      .explorer__header,
      .chart-heading,
      .chart-footer {
        align-items: flex-start;
        flex-direction: column;
      }

      .status {
        text-align: left;
      }

      .controls {
        grid-template-columns: 1fr;
      }

      .controls fieldset {
        grid-column: auto;
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .model-legend__header,
      .model-legend__actions {
        align-items: stretch;
        flex-direction: column;
      }

      .model-legend__actions input {
        width: 100%;
      }

      .model-options {
        max-height: 16rem;
      }

      :global(.model-option) {
        max-width: 100%;
      }

      :global(.metric-chart__heading) {
        align-items: flex-start;
        flex-direction: column;
      }

      :global(.metric-chart__detail) {
        text-align: left;
      }

      .provenance__facts {
        grid-template-columns: 1fr;
      }

      .provenance__facts div,
      .provenance__facts div:nth-child(-n + 2) {
        border-top: 1px solid var(--line);
        border-left: 0;
      }

      .provenance__facts div:first-child {
        border-top: 0;
      }
    }
  </style>

  <script>
    import "../scripts/price-history";
  </script>
</BaseLayout>
